# AISidecar — 한국어 텍스트 스캔 결과

스캔 범위: `artifacts/ai-flow/src/`
스캔 일자: 2026-05-30
탐지 기준: 한글(Hangul) 문자가 포함된 줄

---

## 요약

한국어 텍스트가 들어있는 파일은 **총 7개**입니다. 한국어가 가장 많은 순서:

| 순위 | 파일 | 한국어 줄 수 | 성격 |
|---|------|---------|------|
| 1 | `lib/prompts.ts` | 83줄 | 표준 프롬프트 본문 + 직군/종류 라벨 |
| 2 | `components/Sidecar.tsx` | 64줄 | 사이드바·모달 UI 문구 |
| 3 | `components/TodayDashboard.tsx` | 28줄 | "오늘" 탭 문구·시간 표시 |
| 4 | `components/SaveResultModal.tsx` | 26줄 | 저장 모달 문구 |
| 5 | `components/FileViewModal.tsx` | 15줄 | 파일 보기·복사 래퍼 안내 |
| 6 | `components/SaveReminderToast.tsx` | 2줄 | 저장 알림 토스트 |
| 7 | `App.tsx` | 1줄 | 초기 안내 문구 |

> 참고: "줄 수"는 한글이 1자라도 들어있는 줄의 개수입니다. 한 줄에 여러 문장이 있을 수 있으므로 실제 번역 대상 문구는 이보다 많을 수 있어요.

---

## 파일별 샘플

### 1. `lib/prompts.ts` (83줄) — 가장 큰 덩어리

직군 라벨, 프롬프트 설명, 그리고 **표준 프롬프트 본문**이 전부 한국어입니다. (영어 시장 전환 시 핵심 번역 대상)

```
29:  { id: "development", label: "개발", emoji: "💻", description: "소프트웨어 / 엔지니어링 작업" }
30:  { id: "writing",     label: "글쓰기", emoji: "✍️", description: "소설·에세이·시나리오 등 창작" }
31:  { id: "research",    label: "리서치", emoji: "🔍", description: "조사·분석·가설 검증" }
32:  { id: "design",      label: "디자인", emoji: "🎨", description: "UX·UI·인터랙션" }
33:  { id: "strategy",    label: "전략·기획", emoji: "🧭", description: "제품·전략·의사결정" }
54:  { id: "resume", ... description: "이어서 작업하기" ... }
```
(이 아래로 각 프롬프트의 본문 텍스트가 길게 이어집니다.)

### 2. `components/Sidecar.tsx` (64줄) — UI 본체

```
326: `내용이 비어 있는 파일이 ${empties.length}개 있어요 (${names}). ...`
346: "클립보드가 비어 있어요. AI 응답을 먼저 카피해주세요."
354: "클립보드 읽기 권한이 필요해요. 브라우저 허용 후 다시 시도해주세요."
510: {panelOpen ? "패널 닫기" : "AISidecar"}
526: "클립보드에서 저장"
```

### 3. `components/TodayDashboard.tsx` (28줄) — "오늘" 탭

```
25: if (mins < 1) return "방금";
26: if (mins < 60) return `${mins}분 전`;
28: if (hrs < 24) return `${hrs}시간 전`;
30: if (days < 7) return `${days}일 전`;
112: "프로젝트가 없어요"
```

### 4. `components/SaveResultModal.tsx` (26줄) — 저장 모달

```
273: "결과 저장"
287: "먼저 워크스페이스 탭에서 프로젝트를 만들어주세요."
299: "자동 인식됨"
301: 종류: ... · 
302: 직군: ... ·
```

### 5. `components/FileViewModal.tsx` (15줄) — 파일 보기 / 복사 래퍼 안내

```
39: confirm(`"${file.name}"을 삭제할까요?`)
63: "📎 복사 시 AI가 장황하게 해석하지 않도록 '참고 자료' 안내 문구가 앞뒤에 자동으로 붙습니다."
64: "✓ '참고용으로 읽기만 해줘' 안내와 함께 복사됨. AI에 붙여넣고 다음 지시를 보내세요."
71: "📎 복사 시 'IMMEDIATE 부터 시작해줘' 명령이 자동으로 뒤에 붙어요. ..."
80: "📎 복사 시 '이어서 작업해줘' 안내가 자동으로 뒤에 붙어요."
```

### 6. `components/SaveReminderToast.tsx` (2줄) — 저장 알림

```
83: "한 시간 동안 저장된 게 없어요."
88: aria-label="닫기"
```

### 7. `App.tsx` (1줄) — 초기 안내

```
20: "오른쪽 패널에서 프롬프트를 선택하세요."
```

---

## 참고 — 번역 시 우선순위 제안 (영어 시장 전환 관점)

1. **`lib/prompts.ts`** — 사용자가 AI에 직접 붙여넣는 프롬프트라 가장 중요. 양도 가장 많음.
2. **`Sidecar.tsx` + `SaveResultModal.tsx` + `FileViewModal.tsx`** — 화면에 보이는 버튼·안내 문구.
3. **`TodayDashboard.tsx` + `SaveReminderToast.tsx` + `App.tsx`** — 짧은 상태/시간 문구. 분량 적음.

> ⚠️ 아직 아무것도 수정하지 않았습니다. 요청대로 목록과 샘플만 정리했어요.
