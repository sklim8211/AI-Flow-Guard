# 파일 저장 구현 현황 분석

작성일: 2026-05-27
분석 대상: `artifacts/ai-flow/src/lib/workspace.ts`, `artifacts/ai-flow/src/lib/fsAccess.ts`
**중요: 분석만. 코드 변경 없음.**

---

## 결론 한 줄

**File System Access API 는 이미 구현되어 있고 잘 작동 중.** "마이그레이션" 이라는 표현은 적절하지 않음 — 두 저장소가 **공존**하는 구조이고, 사용자가 폴더 연결 여부로 선택.

---

## 1. 현재 파일이 저장되는 방식

### 두 저장소가 동시에 존재

**(A) localStorage** — 항상 사용. 1차 저장소.
- 위치: `workspace.ts`
- 키: `qq_projects`, `qq_folders`, `qq_files`, `qq_active_project`
- 형태: JSON 문자열로 직렬화
- 데이터 구조: `Project` / `WFolder` / `WFile` 객체들의 배열
- 모든 CRUD 작업이 여기를 통과

**(B) 로컬 폴더 (File System Access API)** — 선택. 2차 저장소(미러).
- 위치: `fsAccess.ts`
- 핸들 보관: IndexedDB (`qq_sidecar_fs` 라는 별도 DB 의 `handles` 스토어)
- 실제 파일: 사용자가 선택한 로컬 폴더 안에 `.md` 파일로 저장
- 폴더 구조: `사용자선택폴더/프로젝트명/CURRENT(or NEXT/ANCHORS/...)/파일명.md`
- 작동 방식: 사용자가 Workspace 탭에서 "폴더 연결" 누르면 → `showDirectoryPicker()` 호출 → 핸들을 IndexedDB 에 저장 → 이후 localStorage 변경 시 같이 디스크에도 기록

### 흐름

```
사용자가 파일 저장
    ↓
ws.createFile / updateFile / deleteFile (localStorage 갱신)
    ↓
폴더 연결되어 있으면 추가로 → fsAccess.writeFile / deleteFile (디스크 갱신)
```

### 지원 환경
- **localStorage**: 모든 브라우저
- **File System Access API**: Chrome / Edge / Opera 만. Firefox / Safari 미지원 (`fsAccess.isSupported()` 로 체크)

---

## 2. "마이그레이션" 이라는 표현이 부정확한 이유

작가님 질문은 "localStorage → File System Access API 로 옮기려면?" 으로 해석되는데, **이미 둘 다 구현되어 있고 같이 작동 중**입니다. 진짜 선택지는 셋:

### 선택지 X — 그대로 두기 (현재)
- localStorage = 항상 1차
- 디스크 = 사용자가 폴더 연결한 경우만 미러
- 장점: Firefox / Safari 사용자도 앱 사용 가능. 폴더 연결 안 해도 동작.
- 단점: 브라우저 데이터 지우면 localStorage 만 쓴 사용자는 다 잃음.

### 선택지 Y — 디스크를 1차로, localStorage 를 캐시로 (진짜 "마이그레이션")
- 디스크에서 매번 읽기, localStorage 는 빠른 조회용 캐시
- 장점: 데이터 손실 위험 거의 0. "진짜 파일" 느낌 강해짐.
- 단점:
  - **Firefox / Safari 사용자는 앱을 못 씀** (또는 별도 처리 필요)
  - 폴더 연결 강제 → 첫 사용 진입 장벽 ↑
  - 모든 읽기 작업이 비동기(async) 가 됨 → 거의 모든 컴포넌트 수정
  - 권한 만료 시 재요청 UX 필요

### 선택지 Z — localStorage 폐기, 디스크만 사용
- 가장 깔끔하지만 Firefox / Safari 완전 차단
- PWA 데스크톱 설치 사용자에게는 큰 영향 없음 (Chrome 기반)
- 모바일 사용자는 거의 못 씀 (모바일 Chrome 은 File System Access API 미지원)

---

## 3. 작업 범위 추정 (선택지 Y 기준 — "진짜 마이그레이션")

### 코드 변경 규모

| 영역 | 변경 내용 | 난이도 |
|------|----------|--------|
| `workspace.ts` 전체 | 모든 CRUD 를 async 로. 디스크 1차 읽기/쓰기. localStorage 는 캐시 갱신만. | 중상 |
| `fsAccess.ts` | 읽기 함수 추가 (`readFile`, `listProjectFiles`, `listFolders`) — 현재는 쓰기만 있음 | 중 |
| 모든 호출처 | `ws.getFiles(...)` → `await ws.getFiles(...)` 로 전부 변경. React 컴포넌트는 useEffect / 로딩 상태 필요 | 상 (파일 10+개 영향) |
| 초기 로드 | 앱 시작 시 디스크 → localStorage 동기화 (긴 작업, 진행률 UI 필요) | 중 |
| 권한 처리 | 권한 거부 시 대체 UX, 핸들 만료 처리, 재요청 | 중 |
| Firefox/Safari 대응 | 명시적 차단 페이지 또는 localStorage 폴백 유지 (이 경우 코드 두 갈래) | 중 |
| 마이그레이션 스크립트 | 기존 localStorage 데이터를 처음 한 번 디스크로 복사 | 하 |
| 동시 변경 충돌 | 외부에서 디스크 파일을 직접 편집한 경우 어떻게 감지·반영할지 (현재 미고려) | 상 |

### 예상 시간
- **최소** (localStorage 폴백 유지하면서 Chrome 만 디스크 1차): **2~3일**
- **완전 전환** (모든 브라우저 지원 포기, 디스크만): **1~2일**
- **숨겨진 비용**: 모든 컴포넌트의 비동기 처리 → 테스트와 엣지 케이스 잡기

### 가장 위험한 부분
1. **외부 편집 동기화** — 사용자가 다른 도구로 디스크 파일을 직접 수정했을 때 우리 앱이 어떻게 알아챌지. 현재 구조엔 이 개념 자체가 없음.
2. **Firefox / Safari 사용자 제거** — 작가님 사용자가 어떤 브라우저인지 데이터 없음.
3. **모바일 사용자 제거** — PWA 를 모바일에서도 쓸 가능성 차단.

---

## 솔직한 권고

**선택지 X (현재) 유지 + 사용자 가이드 강화** 를 추천드립니다. 이유:

1. **현재 구조가 이미 충분히 좋음.** 사용자가 폴더 연결만 하면 디스크에 실제 `.md` 파일로 저장됨. 데이터 안전.
2. **진짜 문제는 코드가 아니라 사용자 행동.** 폴더 연결을 안 하고 쓰는 사용자가 위험에 노출됨. 이건 코드 마이그레이션이 아니라 **온보딩에서 "폴더 연결 강력 추천"** 으로 해결.
3. **마이그레이션 비용이 이득보다 큼.** Firefox/Safari/모바일 사용자 잃음 + 모든 컴포넌트 async 화 + 외부 편집 동기화라는 큰 문제 신규 발생.

**작은 개선 후보 (마이그레이션 없이):**
- 앱 시작 시 폴더 미연결 사용자에게 1회성 안내 배너: "지금 저장한 파일은 이 브라우저에만 있습니다. 컴퓨터 폴더에도 백업하시려면 연결해주세요."
- localStorage 만 사용 중일 때 Workspace 탭 상단에 작은 경고 아이콘
- 이게 진짜 압박이 되면 그때 더 큰 변경 검토

---

## 다음 단계 결정

A. 현재 구조 유지 + 위 "작은 개선" 만 검토
B. 선택지 Y (Chrome 만 디스크 1차, 다른 브라우저는 localStorage 폴백) — 2~3일 작업
C. 선택지 Z (디스크만, 다른 브라우저 차단) — 1~2일 작업, 사용자 잃을 위험
D. 더 자세히 알고 싶은 부분 있음

작가님이 어느 방향 원하시는지 알려주시면 그때 진행합니다. 동결 정책 고려하면 A 가 맞아 보입니다.
